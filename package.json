{
  "name": "org-sim",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "r.js -o build.js && cp src/index.html out && cp lib/require.js out/js && cp -r src/css out",
    "watch": "npm-watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "8.11"
  },
  "watch": {
    "build": {
      "patterns": ["src", "lib"],
      "extensions": "js,html,glsl,css",
      "quiet": false
    }
  },
  "dependencies": {
    "requirejs": "^2.3.5"
  },
  "devDependencies": {
    "npm-watch": "^0.3.0",
    "text": "github:requirejs/text"
  }
}
